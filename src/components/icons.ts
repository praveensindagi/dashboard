/**
 * "Bespoke" means customised, so it has its own icon rather than a pair of scissors.
 * Change it here and the sidebar, metric card and quick action all follow.
 */
export { default as BespokeIcon } from '@mui/icons-material/DesignServicesOutlined'
